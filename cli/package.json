{
  "name": "tau",
  "version": "0.1.0",
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile tau",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
